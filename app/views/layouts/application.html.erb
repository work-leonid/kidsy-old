<!DOCTYPE html>
<html>
  <head>
    <title>Kidsy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="flex gap-20 p-8">

    <p class="notice"><%#= notice %></p>
    <p class="alert"><%#= alert %></p>

    <main class="container mx-auto px-5 flex">
<div>
<% if user_signed_in? %>
  <p class="mb-8">Welcome <%= current_user.email %></p>

    <nav class="flex mb-8 gap-8 flex-col">
      <%= link_to "Journal", journal_items_path %>
      <%= link_to "Todo", todo_items_path %>
    </nav>

<%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete }, id: "sign_out_menu_link", class: "dropdown-item" %>

<% else %>
  <div>
  <%= link_to "Sign up", new_user_registration_path %> |
  <%= link_to "Login", new_user_session_path %>
  </div>

<% end %>
</div>

      <%= yield %>
    </main>
  </body>
</html>
